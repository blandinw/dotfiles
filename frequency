#!/usr/bin/env ruby

counts = {}

ARGF.each do |l|
  line = l.split(';').last
  unless counts[line] 
    counts[line] = 1
  else
    counts[line] += 1
  end
end

counts.sort{|a,b| b[1]<=>a[1]}.each do |k, v|
  puts "#{v} #{k.strip}" if v >= 5
end
